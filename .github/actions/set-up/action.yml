name: Set up project
description: Set up JDK 17 and Gradle

runs:
  using: "composite"
  steps:
    - name: set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '21'
    - name: Set up gradle
      shell: bash
      run: ./gradlew help
